# Plain Node example

This example uses only the dependency-free `next-gallery` entry point.

```bash
pnpm install
pnpm start
```

It prints the calculated image width percentages for a few image aspect ratios. No React or Next.js dependencies are needed.
